/**
 * SecurityAttestations DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: security-attestations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * SecurityAttestations Domain Module - Composition root
 *
 * DDD: Composition root for security-attestations domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AttestationRepositoryAdapter,
} from "@unifora/adapters/security-attestations";
import { getIdGeneratorService } from "@unifora/adapters";
import type { AdapterDynamoDBClient } from "@unifora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetPlatformAttestation,
  ExecuteUpsertPlatformAttestation,
} from "@unifora/services/security-attestations/usecases";
// Ports (interfaces)
import type {
  AttestationRepository,
} from "@unifora/services/security-attestations/ports";

export interface SecurityAttestationsDomainModule {
  repos: {
    attestations: AttestationRepository;
  };
  useCases: {
    attestations: {
      get: ExecuteUpsertPlatformAttestation;
    };
  };
}

export function buildSecurityAttestationsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): SecurityAttestationsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      attestations?: AttestationRepository;
    } = {};

    obj.attestations = new AttestationRepositoryAdapter(dynamoClient);
    return {
      attestations: obj.attestations!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    attestations: {
      get: new ExecuteUpsertPlatformAttestation(executionContext, idGenerator, repos.attestations),
    },
  };
  return { repos, useCases: useCases as SecurityAttestationsDomainModule["useCases"] };
}
