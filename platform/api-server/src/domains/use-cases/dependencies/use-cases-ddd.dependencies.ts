/**
 * UseCases DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: use-cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * UseCases Domain Module - Composition root
 *
 * DDD: Composition root for use-cases domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  DataProductRepositoryAdapter,
  ProgrammePulseRepositoryAdapter,
  TtvGateRepositoryAdapter,
  UseCaseRepositoryAdapter,
} from "@unifora/adapters/use-cases";
import { getIdGeneratorService } from "@unifora/adapters";
import type { AdapterDynamoDBClient } from "@unifora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateUseCase,
  ExecuteGetProgrammePulse,
  ExecuteGetUseCase,
  ExecuteLinkDataProduct,
  ExecuteListUseCases,
  ExecuteSetTTVGate,
} from "@unifora/services/use-cases/usecases";
// Ports (interfaces)
import type {
  DataProductRepository,
  ProgrammePulseRepository,
  TtvGateRepository,
  UseCaseRepository,
} from "@unifora/services/use-cases/ports";

export interface UseCasesDomainModule {
  repos: {
    dataProducts: DataProductRepository;
    programmePulses: ProgrammePulseRepository;
    ttvGates: TtvGateRepository;
    useCases: UseCaseRepository;
  };
  useCases: {
    dataProducts: {
      get: ExecuteLinkDataProduct;
    };
    programmePulses: {
      get: ExecuteGetProgrammePulse;
    };
    ttvGates: {
      update: ExecuteSetTTVGate;
    };
    useCases: {
      create: ExecuteCreateUseCase;
      get: ExecuteGetUseCase;
      list: ExecuteListUseCases;
    };
  };
}

export function buildUseCasesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): UseCasesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      dataProducts?: DataProductRepository;
      programmePulses?: ProgrammePulseRepository;
      ttvGates?: TtvGateRepository;
      useCases?: UseCaseRepository;
    } = {};

    obj.dataProducts = new DataProductRepositoryAdapter(dynamoClient);
    obj.programmePulses = new ProgrammePulseRepositoryAdapter(dynamoClient);
    obj.ttvGates = new TtvGateRepositoryAdapter(dynamoClient);
    obj.useCases = new UseCaseRepositoryAdapter(dynamoClient);
    return {
      dataProducts: obj.dataProducts!,
      programmePulses: obj.programmePulses!,
      ttvGates: obj.ttvGates!,
      useCases: obj.useCases!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    dataProducts: {
      get: new ExecuteLinkDataProduct(executionContext, idGenerator, repos.dataProducts),
    },
    programmePulses: {
      get: new ExecuteGetProgrammePulse(executionContext, idGenerator, repos.programmePulses),
    },
    ttvGates: {
      update: new ExecuteSetTTVGate(executionContext, idGenerator, repos.ttvGates),
    },
    useCases: {
      create: new ExecuteCreateUseCase(executionContext, idGenerator, repos.useCases),
      get: new ExecuteGetUseCase(executionContext, idGenerator, repos.useCases),
      list: new ExecuteListUseCases(executionContext, idGenerator, repos.useCases),
    },
  };
  return { repos, useCases: useCases as UseCasesDomainModule["useCases"] };
}
