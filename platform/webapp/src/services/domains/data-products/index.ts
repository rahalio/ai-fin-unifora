/**
 * data-products domain service barrel (hand-fitted after codegen stub).
 * Network calls go through src/lib/api-client.ts until full service generation is complete.
 */
export const dataProductsDomain = 'data-products' as const;
