/**
 * use-cases domain service barrel (hand-fitted after codegen stub).
 * Network calls go through src/lib/api-client.ts until full service generation is complete.
 */
export const useCasesDomain = 'use-cases' as const;
