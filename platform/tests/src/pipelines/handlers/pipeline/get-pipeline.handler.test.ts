/**
 * GetPipeline handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: pipelines
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getPipeline } from "@unifora/api-server/domains/pipelines/handlers";
import type { PipelinesDDDDependencies } from "@unifora/api-server/domains/pipelines";
import { createPipeline as createPipelineEntity } from "@unifora/factories/pipelines";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@unifora/tests/shared";
import { PIPELINES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetPipeline", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PipelinesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PipelinesDDDDependencies>(
      PIPELINES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createPipelineEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetPipeline, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { pipelineId: entity.id };

    await getPipeline(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetPipeline.execute).toHaveBeenCalledWith(expect.objectContaining({ pipelineId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { pipelineId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetPipeline, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getPipeline(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
