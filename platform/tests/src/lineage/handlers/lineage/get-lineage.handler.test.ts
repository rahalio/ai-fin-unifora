/**
 * GetLineage handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: lineage
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getLineage } from "@unifora/api-server/domains/lineage/handlers";
import type { LineageDDDDependencies } from "@unifora/api-server/domains/lineage";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@unifora/tests/shared";
import { LINEAGE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetLineage", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: LineageDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<LineageDDDDependencies>(
      LINEAGE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetLineage, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { dataProductId: entity.id };

    await getLineage(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetLineage.execute).toHaveBeenCalledWith(expect.objectContaining({ dataProductId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { dataProductId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetLineage, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getLineage(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
