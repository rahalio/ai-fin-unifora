/**
 * GetDataProduct handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: data-products
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDataProduct } from "@unifora/api-server/domains/data-products/handlers";
import type { DataProductsDDDDependencies } from "@unifora/api-server/domains/data-products";
import { createDataProduct as createDataProductEntity } from "@unifora/factories/data-products";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@unifora/tests/shared";
import { DATAPRODUCTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDataProduct", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DataProductsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DataProductsDDDDependencies>(
      DATAPRODUCTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createDataProductEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDataProduct, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { dataProductId: entity.id };

    await getDataProduct(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDataProduct.execute).toHaveBeenCalledWith(expect.objectContaining({ dataProductId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { dataProductId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDataProduct, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDataProduct(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
