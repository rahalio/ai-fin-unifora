/**
 * GetReadinessScore handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: readiness
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getReadinessScore } from "@unifora/api-server/domains/readiness/handlers";
import type { ReadinessDDDDependencies } from "@unifora/api-server/domains/readiness";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@unifora/tests/shared";
import { READINESS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetReadinessScore", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ReadinessDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ReadinessDDDDependencies>(
      READINESS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetReadinessScore, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { useCaseId: entity.id };

    await getReadinessScore(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetReadinessScore.execute).toHaveBeenCalledWith(expect.objectContaining({ useCaseId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { useCaseId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetReadinessScore, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getReadinessScore(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
