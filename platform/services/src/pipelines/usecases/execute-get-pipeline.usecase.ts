/**
 * ExecuteGetPipeline - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: pipelines
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetPipeline Use Case
 *
 * DDD: Application use case for get Pipeline.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetPipelineInput, GetPipelineOutput } from "../dto/pipeline.dto";
import type { ExecutionContextService, IdGeneratorService } from "@unifora/services/_shared/index.js";
import type { PipelineRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetPipeline {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly pipeline: PipelineRepository
  ) {}

  async execute(input: GetPipelineInput): Promise<GetPipelineOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.pipId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).pipelineId) {
      throw new ValidationError("pipelineId is required");
    }
    const pipelineId = (input as any).pipelineId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.pipeline.getPipeline({ pipelineId: pipelineId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${pipelineId}`);
    }

    return entity as GetPipelineOutput;
  }
}
