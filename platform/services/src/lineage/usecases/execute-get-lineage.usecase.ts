/**
 * ExecuteGetLineage - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: lineage
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetLineage Use Case
 *
 * DDD: Application use case for get Lineage.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetLineageInput, GetLineageOutput } from "../dto/lineage.dto";
import type { ExecutionContextService, IdGeneratorService } from "@unifora/services/_shared/index.js";
import type { LineageRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetLineage {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly lineage: LineageRepository
  ) {}

  async execute(input: GetLineageInput): Promise<GetLineageOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.linId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).dataProductId) {
      throw new ValidationError("dataProductId is required");
    }
    const dataproductId = (input as any).dataProductId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.lineage.getLineage({ dataProductId: dataproductId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${dataproductId}`);
    }

    return entity as GetLineageOutput;
  }
}
