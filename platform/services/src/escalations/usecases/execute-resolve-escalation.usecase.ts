/**
 * ExecuteResolveEscalation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: escalations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteResolveEscalation Use Case
 *
 * DDD: Application use case for get Resolve.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ResolveEscalationInput, ResolveEscalationOutput } from "../dto/resolve.dto";
import type { ExecutionContextService, IdGeneratorService } from "@unifora/services/_shared/index.js";
import type { ResolveRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteResolveEscalation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly resolve: ResolveRepository
  ) {}

  async execute(input: ResolveEscalationInput): Promise<ResolveEscalationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.escId();

    // All field names extracted from OpenAPI spec: grantShare, resolution
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).escalationId) {
      throw new ValidationError("escalationId is required");
    }
    const escalationId = (input as any).escalationId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.resolve.resolveEscalation({ escalationId: escalationId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${escalationId}`);
    }

    return entity as ResolveEscalationOutput;
  }
}
