/**
 * ExecuteCreateUniforaAuditExport - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: audits
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteCreateUniforaAuditExport Use Case
 *
 * DDD: Application use case for create Export.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { CreateUniforaAuditExportInput, CreateUniforaAuditExportOutput } from "../dto/export.dto";
import type { ExecutionContextService, IdGeneratorService } from "@unifora/services/_shared/index.js";
import type { ExportRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteCreateUniforaAuditExport {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly exportRepo: ExportRepository
  ) {}

  async execute(input: CreateUniforaAuditExportInput): Promise<CreateUniforaAuditExportOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.audId();

    // All field names extracted from OpenAPI spec: periodFrom, periodTo, scope
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!input.periodFrom) {
      throw new ValidationError("periodFrom is required");
    }
    if (!input.periodTo) {
      throw new ValidationError("periodTo is required");
    }    // Check policy
    if (!CanCreate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Generate entity ID in services layer using IdGeneratorService (audits domain)
    const entityId = this.idGenerator.audId();

    // Create entity via repository (pass ID and correlation ID for response meta)
    const entity = await this.exportRepo.createUniforaAuditExport({ ...input, id: entityId, createdByActorId: (input as any).createdByActorId ?? this.context.getUserId(), createdByActorType: (input as any).createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId } as any);

    return entity as CreateUniforaAuditExportOutput;
  }
}
