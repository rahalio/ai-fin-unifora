/**
 * ExecuteListCapacityPlans - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: capacity
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListCapacityPlans Use Case
 *
 * DDD: Application use case for list Plan.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListCapacityPlansInput, ListCapacityPlansOutput } from "../dto/plan.dto";
import type { ExecutionContextService, IdGeneratorService } from "@unifora/services/_shared/index.js";
import type { PlanRepository } from "../ports";

export class ExecuteListCapacityPlans {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly plan: PlanRepository
  ) {}

  async execute(input: ListCapacityPlansInput): Promise<ListCapacityPlansOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.capId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.plan.listCapacityPlans({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListCapacityPlansOutput;
  }
}
