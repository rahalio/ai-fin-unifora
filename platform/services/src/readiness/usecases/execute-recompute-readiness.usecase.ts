/**
 * ExecuteRecomputeReadiness - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: readiness
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRecomputeReadiness Use Case
 *
 * DDD: Application use case for get Readiness.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RecomputeReadinessInput, RecomputeReadinessOutput } from "../dto/readiness.dto";
import type { ExecutionContextService, IdGeneratorService } from "@unifora/services/_shared/index.js";
import type { ReadinessRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRecomputeReadiness {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly readiness: ReadinessRepository
  ) {}

  async execute(input: RecomputeReadinessInput): Promise<RecomputeReadinessOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.rdyId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).useCaseId) {
      throw new ValidationError("useCaseId is required");
    }
    const usecaseId = (input as any).useCaseId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.readiness.recomputeReadiness({ useCaseId: usecaseId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${usecaseId}`);
    }

    return entity as RecomputeReadinessOutput;
  }
}
