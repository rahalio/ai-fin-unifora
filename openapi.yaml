# Unifora OpenAPI specs have moved to packages/openapi-core/src/
#
# Domain YAML pairs (OpenAPI 3.1.0):
#   identity.yaml + identity.schemas.yaml
#   use-cases.yaml + use-cases.schemas.yaml
#   data-products.yaml + data-products.schemas.yaml
#   shares.yaml + shares.schemas.yaml
#   readiness.yaml + readiness.schemas.yaml
#   pipelines.yaml + pipelines.schemas.yaml
#   escalations.yaml + escalations.schemas.yaml
#   lineage.yaml + lineage.schemas.yaml
#   audits.yaml + audits.schemas.yaml
#   capacity.yaml + capacity.schemas.yaml
#   security-attestations.yaml + security-attestations.schemas.yaml
#
# Shared components: packages/openapi-core/src/common/
#
# Bundle all domains:
#   pnpm --filter @unifora/openapi-core bundle:domains
#
# Lint:
#   pnpm --filter @unifora/openapi-core lint:domains
#
# See also: docs/CODEGEN.md, PRODUCT.md
